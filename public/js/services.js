angular.module('cheaperChoice.services', []).
	
	factory('initAutocomplete', [function(){
	  	return function($scope) {
	  		// stuff
	  	}	
	}]).

	factory('mapCoordinates', [function(){
	  	return function($scope) {
	  		// stuff
	  	}	
	}]).

	factory('renderGoogleMap', [function(){
	  	return function($scope) {
	  		// stuff
	  	}	
	}]).

	factory('getRidePrices', [function(){
	  	return function($scope) {
	  		// stuff
	  	}	
	}]).

	factory('getCurrentTime', [function(){
	  	return function($scope) {
	  		// stuff
	  	}	
	}]);
